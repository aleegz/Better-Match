/*import { fetchData } from './api.js';

async function loadComponentData() {
  try {
    const data = await fetchData(); // Aquí se ejecuta el código en api.js y los console.log() se mostrarán en la consola
    // Realizar acciones con los datos obtenidos, si es necesario.
  } catch (error) {
    // Manejar errores si es necesario.
  }
}

// Llamar a la función para cargar los datos cuando sea necesario
loadComponentData();

export default loadComponentData;*/